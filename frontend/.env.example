# Leave this as a relative path when using the Vite development proxy.
VITE_API_BASE_URL=/api/v1

# Optional for the single-tenant demo; backend defaults are used when omitted.
VITE_ORGANIZATION_ID=org_demo
VITE_PROJECT_ID=project_demo
VITE_ACTOR_ID=demo-user
